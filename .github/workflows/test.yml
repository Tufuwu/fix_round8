name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.8]
        clickhouse-version: [19.3.8, 18.14.9]

    services:
      clickhouse:
        image: yandex/clickhouse-server:${{ matrix.clickhouse-version }}
        ports:
          - 9000:9000
          - 8123:8123
        options: --ulimit nofile=262144:262144

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip setuptools
        pip install flake8 flake8-print coveralls pytest pytest-cov sqlalchemy responses mock parameterized

    - name: Override setup.cfg
      run: sed -i 's/^host=localhost$/host=127.0.0.1/' setup.cfg

    - name: Update /etc/hosts
      run: echo '127.0.0.1 clickhouse-server' | sudo tee -a /etc/hosts

    - name: Lint with flake8
      run: flake8

    - name: Test with coverage
      run: pytest --cov=clickhouse_sqlalchemy

    - name: Coveralls
      env:
        COVERALLS_REPO_TOKEN: ${{ secrets.COVERALLS_REPO_TOKEN }}
      run: coveralls
